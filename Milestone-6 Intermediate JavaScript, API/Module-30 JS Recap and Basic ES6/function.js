function sum(num1 = 0, num2 = 0) {
    const result = num1 + num2;
    console.log(result);
}

sum(10);
console.log(sum.length);
// function_name.length means how many parameter have in this function.but if i set default parameter then length will be zero(0)
